version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: my_go_app    
    volumes:
      - ./lib:/app
    ports:
      - "8080:8080"


# Extender la configuración con otros archivos
x-include:
  - compose.dev.yml
  - compose.prod.yml
  - compose.test.yml
